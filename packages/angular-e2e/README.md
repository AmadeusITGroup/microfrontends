# E2E test cases for the core package

This package contains the e2e test cases for the `core` package.

## Running the tests

- `npm run -w core-e2e test` to run the tests
- `npm run -w core-e2e tdd` to run the tests in watch mode and Playwright UI
